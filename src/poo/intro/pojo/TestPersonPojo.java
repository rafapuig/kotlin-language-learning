import poo.intro.pojo.kotlin.Person;

void main() {
    // Creamos una instancia de la clase Person definida en Kotlin
    var person = new Person("Perico Palotes");
    IO.println(person.getName());
}