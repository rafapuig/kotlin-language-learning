package poo.intro.pojo.kotlin

/**
 * Equivalente a la clase Person escrita en Java
 *
 * (No es necesario el modificador public, la clase en Kotlin ya lo es por defecto)
 */
class Person(val name: String)